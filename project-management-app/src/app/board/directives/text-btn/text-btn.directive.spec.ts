import { TextBtnDirective } from './text-btn.directive';

describe('TextBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBtnDirective();
    expect(directive).toBeTruthy();
  });
});
